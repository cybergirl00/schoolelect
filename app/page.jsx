import LandingPage from "@/components/screens/LandingPage";

const page = () => {
  const user = false;

  // if(!user) return <LandingPage />


  return (
    <div>
      <LandingPage />
    </div>
  )
}

export default page